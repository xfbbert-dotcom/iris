# Task 8 Implementation And Evidence Report

## Status

`NEEDS_CONTEXT`

All code, documentation, local verification, exact-SHA CI, backup, migration, private deployment,
and disabled public-boundary gates are complete. Real Feishu citation and permission-revocation
acceptance is not complete. Iris remains fail-closed; no real-pilot evidence has been invented.

## Intended Commits

- `940463ace17d24baf71603e72cd0b74295826ab3` - `fix(core): require answer reply status counts`
- `adac01cd2d2f4cd2ef01ed089a60719efa354629` -
  `docs: add answer citation acceptance gates`
- The final evidence/report commit is the commit containing this file and is reported in the
  controller-facing Task 8 status.

The candidate deployed for acceptance is
`adac01cd2d2f4cd2ef01ed089a60719efa354629`. It contains all runtime code and the bounded rollout
documentation. The later evidence-only commit does not change deployed application code.

## Deferred Minor Cleanup

The two deferred progress-ledger minors were resolved with TDD before full verification:

1. Type-level tests first required `answerReplyUnresolvedCount`,
   `answerReplyPendingSafeNoticeCount`, and `answerReplyReconciliationRequiredCount`. Typecheck
   failed while the fields were optional, then passed after `EventWorkerRuntimeStatus` made all
   three required and typed fixtures were updated.
2. The enabled-responder test now explicitly requires
   `mentionRepliesUnavailableReason` to be absent. Injecting an own property with value
   `undefined` made the focused test fail; removing the mutation restored the green result.

## Local Verification

Database verification used the local Docker test PostgreSQL database specified by the task. No
answer-reply database test skipped.

- Focused feature suite: 9 files, 350 passed, 0 skipped.
- Core full suite: 165 files, 3,056 passed, 16 conditional skips.
- All 70 PostgreSQL answer-reply repository tests ran.
- Conditional full-suite skips: 6 legacy tests gated by generic `DATABASE_URL`, 3 mixed
  Postgres/Redis tombstone tests awaiting `IRIS_TEST_REDIS_URL`, and 7 Redis-only runtime/queue
  tests.
- Python suite: 181 passed.
- Pilot operations, backup, and restore suite: 141 passed, 0 skipped.
- Typecheck, production build, `git diff --check`, root Compose rendering, pilot configuration,
  and pilot Compose rendering: exit 0.
- Readiness: `ready`, 16 passed, 0 warnings, 0 failures.

The focused suite was run with both `IRIS_TEST_DATABASE_URL` and the legacy `DATABASE_URL` pointed
at the same local Docker test database so its legacy document-fragment block also ran. The full
Core suite used only `IRIS_TEST_DATABASE_URL`; setting generic `DATABASE_URL` changes unrelated
runtime-composition tests, and their focused rerun with the requested test variable passed 15/15.

## Scope And Data Boundary

The baseline and Task 8 diffs contain only planned answer-citation, answer-receipt, integration,
test, plan, runbook, and evidence files. Immutable source and event records contain no body text.
The private API maps its response explicitly and does not spread the delivery row. Searches found
no `fragmentText`, `promptContext`, `appSecret`, or `tenantAccessToken` in the answer-reply module;
the only `preparedReplyText` references are the intended mutable delivery-path field, validator,
and service, with terminal clearing covered by tests.

The observed Task 8 artifacts `.tmp-iris-backup-test-yG81NG` and
`.tmp-iris-smoke-test-mNElXe` were generated by pilot tests and self-cleaned. A later bounded test
artifact `.tmp-iris-backup-test-zYy9d0` also self-cleaned. The older ignored
`.tmp-iris-backup-test-dOJeSY` predates Task 8 and was left untouched. No Task 8 temp artifact or
unrelated untracked file is present in the final intended worktree.

## GitHub Publication And CI

- Draft PR: <https://github.com/xfbbert-dotcom/iris/pull/23>
- Base: `codex/iris-chat-knowledge-drafts`
- Candidate head: `adac01cd2d2f4cd2ef01ed089a60719efa354629`
- Core CI: success,
  <https://github.com/xfbbert-dotcom/iris/actions/runs/30746470862/job/91492812254>
- AI Worker CI: success,
  <https://github.com/xfbbert-dotcom/iris/actions/runs/30746470862/job/91492812226>
- PR #22: open, draft, and unmerged.
- PR #23: open, draft, and unmerged.

## Fail-Closed Deployment Evidence

The production repository is checked out at candidate SHA
`adac01cd2d2f4cd2ef01ed089a60719efa354629`. The approved marker remains
`b25d8298fd396366c97449dfbe6f11f3dc42f8f9`; the candidate has not been promoted.

- Pre-deploy runtime was durably disabled and Caddy stopped before backup, migration, or candidate
  startup.
- PostgreSQL backup `iris-20260802T115722Z.bundle.tar.age` completed first; size 28,228,520 bytes.
- `0045_answer_source_citations.sql` is present in `schema_migrations`.
- Running images are exactly
  `iris-core:adac01cd2d2f4cd2ef01ed089a60719efa354629` and
  `iris-ai-worker:adac01cd2d2f4cd2ef01ed089a60719efa354629`.
- Private Core status is healthy and live readiness is `ready`.
- Event worker is running, mention replies are enabled, and
  `mentionRepliesUnavailableReason` is absent.
- Private unknown-receipt probe returned 404 without content disclosure.
- During a bounded disabled-ingress window, public `/health` returned 200 and
  `/internal/answer-replies/feishu/probe` returned 404.
- After the boundary check, `globalEnabled=false`, `desiredGlobalEnabled=false`, and Caddy is
  stopped.
- Event, document-sync, and reindex pending and DLQ counts are zero.
- `answerReplyUnresolvedCount=0`, `answerReplyPendingSafeNoticeCount=0`, and
  `answerReplyReconciliationRequiredCount=0`.

The production-image environment-only readiness CLI cannot evaluate enabled live runtime
components without a live readiness URL, so it reported the expected missing runtime statuses
while ingress remained stopped. Candidate deployment then passed the required private live
`/internal/readiness` gate. Three wrapper-level public-boundary attempts produced no acceptance
evidence because of a status-path assertion and shell transport/Compose invocation issues; every
attempt stopped Caddy. The corrected bounded check passed and restored the verified fail-closed
state.

## Remaining Pilot Gate

No new user-owned Wiki fixture exists yet, so enabling the pilot would violate the acceptance
runbook. The exact next single human action is:

> Create a new Feishu Wiki page titled `Iris Citation Pilot 2026-08-02 2010 CST`, put the marker
> `IRIS-CITATION-PILOT-20260802-2010-CST` plus bounded non-sensitive test text in its body, share
> that page with the Iris app, and return its canonical Feishu URL.

After that one action, Task 8 must resume from sync/index/permission verification, enable only the
existing pilot group for one real answer, capture the incoming message ID and visible cited reply,
inspect the content-free receipt, perform the permission-revocation check, prove the final zero
state, and only then decide whether to restore the previously approved pilot runtime. Until then,
the deployment remains globally disabled with Caddy stopped and both draft PRs unmerged.
